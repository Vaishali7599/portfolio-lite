import React from "react";


const Section = ({heading, description}) => {
    return (
        <div>
            
            <h1>{heading}</h1>
            <p>{description}</p>
        </div>
    )
}
export default Section;